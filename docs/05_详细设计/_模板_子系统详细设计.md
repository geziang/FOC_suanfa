# XX 子系统详细设计（复制本模板新建分册）

> 状态：草稿 ｜ 对应代码：`src/xxx/Xxx.h`、`src/xxx/Xxx.cpp`
> 最后对齐：【待填写：git 短号或日期】 ｜ 上游规格：【待填写：SPEC-P 条款 / ARC-01 契约】

> 使用说明：新建分册时复制本文件并改名为 `序号_子系统名详细设计.md`，并在 `00_文档总览.md` 台账登记一行；逐节填写，删除本使用说明；未确定处保留 `【待填写】` 引导块；详细设计只写 why/契约/不变量/取舍，不逐行抄代码。

## 1. 职责与边界

- 一句话职责：【待填写】
- 负责：【待填写】
- **明确不做**：【待填写：列出非目标，划清与相邻层的边界（参照 ARC-01 契约）】
- 上游依赖 / 下游被谁调用：【待填写：接口层只允许向下依赖】

## 2. 架构位置

> 【待填写：本子系统在五层架构中的位置与数据流；标明周期任务（loop() 控制拍）与初始化路径（setup()）边界】

```mermaid
flowchart LR
  A[上游] --> B[本子系统] --> C[下游]
```

## 3. 文件清单

| 文件 | 角色（一文件一行） |
| --- | --- |
| `src/xxx/xxx.h/.cpp` | 【待填写】 |

## 4. 关键数据结构

> 记录结构体字段、单位（SI，见 ARC-01 §5）、取值范围、谁写谁读、生命周期；以代码为准，这里解释设计意图。

| 结构/字段 | 单位 | 范围 | 写入者 | 读取者 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 【待填写】 | | | | | |

## 5. 关键流程与状态机

> 【待填写：模式切换（Idle/Torque/Velocity/Position）、时序、控制拍内调用顺序（update 的先后依赖）；可引用状态图】

## 6. 关键机制与取舍（核心）

> 【待填写：为什么这样设计、否决了什么备选。例："力矩模式下为何自行刷新 shaft_velocity""torqueToVolts 为何放在适配层而非让 SimpleFOC 换算"】

- 机制 1：【待填写】
- 被否决方案及原因：【待填写】

## 7. 对外接口契约

| 函数签名 | 前置条件 | 后置/返回 | 上下文(setup/loop) | 可重入 |
| --- | --- | --- | --- | --- |
| 【待填写】 | | | | |

## 8. 配置与阈值

> 阈值集中在 bsp 配置或本模块头，禁止散落；记录量纲、默认值来源（规格书/官方例程/辨识）、整定方法。

| 参数 | 量纲 | 默认值 | 来源 | 整定方法 |
| --- | --- | --- | --- | --- |
| 【待填写】 | | | | |

## 9. 资源预算

- RAM（静态/缓冲）：【待填写】
- Flash（代码量）：【待填写】
- CPU（控制拍内最坏耗时，是否含三角函数/I2C 阻塞读）：【待填写】

## 10. 验证与追溯

| 验证手段 | 覆盖内容 | 对应规格条款 |
| --- | --- | --- |
| 上板验收 / 串口 stream 数据 | 【待填写】 | 【待填写：SPEC-P P0-x】 |

## 11. 非目标、已知限制、TODO

- 非目标：【待填写】
- 已知限制：【待填写】
- TODO：【待填写】
