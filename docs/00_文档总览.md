# 00 · 文档总览（地图 / 台账 / 规则）

本文件是整个 `docs/` 的入口：文档分层、台账、状态与追溯规则、维护纪律。文档体系移植自实验室关节电机项目（STM32 底层）的标准，两个项目共享同一套方法论。项目门面见根目录 `README.md`。

---

## 1. 文档分层模型

文档按抽象层级分目录，**下层服从上层，同层不互相重复**：

| 层 | 目录 | 回答的问题 | 变化速度 |
| --- | --- | --- | --- |
| L1 需求与选型 | `01_需求与选型/` | 为什么做、边界在哪、什么纪律 | 最慢 |
| L2 架构设计 | `02_架构设计/` | 分几层、契约是什么、单位与符号 | 慢 |
| L3 规格与计划 | `03_规格与计划/` | 什么算通过（验收合同） | 中 |
| L4 硬件适配 | `04_硬件适配/` | 引脚真值、电机档案、参数歧义 | 硬件定后基本冻结 |
| L5 详细设计 | `05_详细设计/` | 代码怎么落地：机制、取舍、资源预算 | 随代码演进 |
| L6 验证记录 | `06_验证与实验记录/` | 实测证据、验收报告、经验复盘 | 持续追加 |
| 参考 | `07_参考资料/`、`assets/` | 外部资料索引、图片 | 按需 |

**核心边界**：L3 是"合同/验收标准"（稳定），L5 是"施工方案"（随代码更新），代码本身是 how 的最终事实。详细设计只写 why、契约、不变量与取舍，**不逐行复述代码**。

## 2. 文档台账（新增文档必须在此登记）

| 编号 | 文档 | 层 | 职责 | 状态 | 上游 |
| --- | --- | --- | --- | --- | --- |
| REQ-01 | `01_需求与选型/项目目标与总体方案.md` | L1 | 背景与软件先行决策、定位边界、工业开发规范 | 已对齐 | — |
| ARC-01 | `02_架构设计/FocKit分层架构与IMotor契约.md` | L2 | 五层架构、IMotor 分层契约、移植路径、术语与单位 | 已对齐 | REQ-01 |
| SPEC-P | `03_规格与计划/阶段验收计划_P0-P4.md` | L3 | P0~P4 阶段验收合同（P0 条目含测试方法与判据） | P0 已对齐待上板；P1~P4 草稿 | ARC-01 |
| HW-DENG | `04_硬件适配/DengFOC_V4与2208套件档案.md` | L4 | DengFOC V4 引脚真值、2208 档案、参数歧义登记、上电安全 | 已对齐 | — |
| DD-01（预留） | `05_详细设计/01_适配层与标定固化详细设计.md` | L5 | SimpleFocMotor / MotorManager / NVS 固化 | 待建（P0 上板验收后立项） | ARC-01、SPEC-P |
| DD-02（预留） | `05_详细设计/02_三环中间件详细设计.md` | L5 | PID / VelocityNode / PositionNode | 待建 | SPEC-P |
| DD-03（预留） | `05_详细设计/03_串口调参壳详细设计.md` | L5 | SerialShell | 待建 | — |

## 3. 文档状态约定

每份文档文头必须标注状态，四档：

- **草稿**：正在写，结构和内容可能大改；
- **已对齐**：与当前代码/事实一致，可作为依据；
- **滞后**：代码已变、文档未跟，仅作历史参考，需更新或并入；
- **废弃**：不再适用，保留留痕，不再维护。

调试日志另有 **OPEN（挂账）** 状态：问题未闭环、判决实验未做，禁止提前写结论。

## 4. 编号与三层追溯规则

- 文档编号：`REQ / ARC / SPEC / HW / DD / TST（验收记录）/ EXP（经验帖）-序号`，分册内小节用 `SPEC-P§2` 形式引用；
- **三层追溯链**：`规格条款(What) ↔ 详细设计(How) ↔ 代码符号(Where)`。
  - 例：`SPEC-P P0-④ ↔ DD-01§标定固化 ↔ SimpleFocMotor::init 的 store_.load 注入`；
- 互引用**稳定编号或函数名，不写行号**（行号随代码变动失效）；
- 每个详细设计分册末节必须回填它实现了哪些规格条款。

## 5. 维护规则（防腐烂，写入完成定义 DoD）

1. 代码是 how 的单一事实源，详细设计只写设计意图、契约、不变量、被否决的方案；
2. 改代码后必须检查对应分册，状态变化时更新文头"最后对齐"与本台账；
3. 新增子系统：复制 `05_详细设计/_模板_子系统详细设计.md` 建分册，并在本台账登记一行；
4. 未确定内容统一用 `> 【待填写】……` 引导块，全文可检索 `【待填写】` 找到所有未完成点；
5. **分组收敛**：详细设计只按"已存在的代码簇"分册；尚未验收的能力在相关分册内用占位小节预留，不新建文件、不新开目录（DD-01~03 因此挂"待建"）。

## 6. 推荐阅读路径

- 理解全局：REQ-01 → ARC-01；
- 接硬件跑示例：HW-DENG → SPEC-P §1 → `06_验证与实验记录/`；
- 改某模块：本台账定位分册 → 对应 DD → 代码符号；
- 将来移植到实验室硬件：ARC-01 §4 移植路径 → HW 新板档案。

## 7. 变更记录

### 2026-09-16：文档库建立

- 文档体系自实验室关节电机项目（STM32 底层）标准移植：L1~L6 分层、台账、状态四档、三层追溯、模板与 `_` 前缀元文件、经验帖序列全部沿用；
- 原 `docs/00_项目目标与总体方案.md`（v0.1.0 合并版）无损拆分为 REQ-01 / ARC-01 / SPEC-P / HW-DENG 四份分层文档，原文件删除；
- SPEC-P 将 P0 验收标准从路线图表格扩展为含测试方法/判据/记录位置的验收合同；P0-①~⑤ 对应代码已就绪，待上板；
- README 文档索引改指本文件；`05_详细设计` 按分组收敛规则只建模板、不预写分册。
