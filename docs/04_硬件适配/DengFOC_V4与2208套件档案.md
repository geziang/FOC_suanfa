# DengFOC V4 与 2208 套件档案

> 状态：已对齐（引脚真值源自官方例程实测，非记忆值）｜ 文档编号：HW-DENG
> 最后对齐：2026-09-16（自合并版 docs/00 v0.1.0 拆分）｜ 上游：—
> 对应代码：`src/bsp/DengFocBoard.h`（板级单一事实源，换板只改该文件）

---

## 1. DengFOC V4 板级真值

来源：`v4/2、ESP有感FOC历程资料/Simplefoc库例程/` 第 1/3/8 号例程（测试库 SimpleFOC 2.2.1）。

| 资源 | 参数 |
|---|---|
| 主控 | ESP32-WROOM-32U（板载双路三相半桥） |
| M0 相线 | A=32，B=33，C=25 |
| M1 相线 | A=26，B=27，C=14 |
| 驱动使能 | 12（双路共用） |
| M0 编码器 I2C | `TwoWire(0)`，SDA=19，SCL=18 |
| M1 编码器 I2C | `TwoWire(1)`，SDA=23，SCL=5 |
| 双编码器说明 | AS5600 地址固定 0x36，双编码器**必须**走两路独立总线 |
| VIN 检测 | 引脚 13，`analogReadMilliVolts(13) × 8.5 / 1000` |
| 欠压阈值 | 11.1 V（欠压失使能，恢复后自动解除） |
| 板载电流采样 | InlineCurrentSense(0.01Ω shunt × 50V/V 增益)，M0 引脚 A=39、B=36（16 课例程；M1 引脚待查 15 课，P1 电流环立项时补齐）——P1"实测 iq"的硬件基础 |

## 2. 2208 云台电机（规格书）

| 参数 | 值 |
|---|---|
| 槽极数 | 12N14P，**极对数 7**（官方例程 `BLDCMotor(7)` 一致） |
| 绕线电阻 | 21.2 Ω（"绕线电阻"，星形接法） |
| 相电感 | 5.3 mH |
| KV 值 | 110 KV |
| 扭矩 | 约 0.03 N·m（300 g·cm） |
| 额定/最大电流 | 0.8 A / 4.5 A |
| 适用电压 | DC 7.4~16 V，额定 12 V |
| 编码器 | AS5600，12 bit，0.087°，I2C（默认），3.3V |

## 3. 参数歧义登记（P1 辨识收口，对应 SPEC-P §2）

| 参数 | 规格书值 | 歧义 | 当前默认（bsp） |
|---|---|---|---|
| 相电阻 | 21.2 Ω "绕线电阻" | 线间还是相电阻 | 10.6 Ω（按星形线间/2 解读；若实测 21.2 即相阻，改回 21.2） |
| 相电感 | 5.3 mH | 同上 | 2.65 mH |
| 力矩常数 KT | 0.03 N·m | 对应额定还是峰值电流；KV 换算约 0.012~0.02 | 0.03 N·m/A |

## 4. 上电与安全注意

1. **上电仪式**：任何 `driver.init()` 之前必须 `dengfoc_v4::earlyInit()`——相线输入上拉 + 12bit ADC（官方例程同款，库内已内置到示例 setup）；
2. **供电**：12V 额定；串口会阻塞等待电压 ≥11.1V 才继续初始化（官方流程同款）；
3. **开环警告**：官方对开环模式 `voltage_limit=2V` 有强烈警告（长时间过大电压电流可能烧板），本库闭环限压 = 额定电流×相电阻 ≈ 8.5V，进一步放宽须有数据支撑；
4. **使能共用**：使能脚 12 双路共用，双电机切换控制时注意两路桥臂状态（MotorManager 切换时自动失能旧电机）；
5. **首次标定**：initFOC 标定时电机会有小幅转动，保持转子自由。

## 5. 工具链环境（已配置，2026-09-17 登记）

| 组件 | 版本 | 说明 |
|---|---|---|
| Arduino IDE | 已安装 | 用户环境 |
| ESP32 board package | 2.0.4 | 与官方例程实测版本一致（离线包见 `v4/1、V4到手资料/`） |
| SimpleFOC 库 | 2.2.1 | 官方例程实测版本，FocKit 按此版本 API 开发 |
| 上位机 | SimpleFOCStudio | 图形化调参（本地 EXE 版）；固件对接 = `src/adapter/StudioBridge`（16 课 Commander 模式），见示例 04 |
