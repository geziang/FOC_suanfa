# 阶段验收计划 P0~P4

> 状态：**P0 已回填（2026-09-22，事实证据口径）**；**P1 三环已全验收（电流 TST-01 / 速度 TST-02 / 位置 TST-03，2026-09-21）**；**P2 力控已立项（2026-09-22 文档先行，四件收官口径：T-P2-1/2/3/5，判据草案见 §3 与 REF-14）**；其余 P1 细项与 P3~P4 草稿 ｜ 文档编号：SPEC-P
> 最后对齐：2026-09-22（P0 回填+红线勘正；P2 立项并扩为四件（T-P2-3 虚拟墙/T-P2-5 柔顺轨迹）+砍件留痕+两轮交付切分；前一里程碑 2026-09-21 P1 三环收官）｜ 上游：REQ-01、ARC-01、SPEC-T

---

## 1. P0 套件载体（现行阶段）

代码已全部就绪（git `28a66ef`，FocKit v0.1.0），验收条目如下。**每条验收后按 `06_验证与实验记录/验收报告/` 模板落记录，本表回填实测状态。**

> **2026-09-22 回填说明**：P0 序列未举行独立验收仪式，其实质内容在 P1 整定战役（2026-09-18~21）期间全部事实执行完毕——状态按"事实证据 / 被更严验收取代"口径回填，未单独补立报告；P0-③ 判据按 TST-02 边界知识修订（判据写在实测知识之前的典型案例）。

| 条目 | 内容 | 测试方法 | 通过判据 | 记录位置 | 状态 |
|---|---|---|---|---|---|
| P0-① | 主程序编译烧录 | Arduino IDE（ESP32 core ≥2.0.4 + Simple FOC ≥2.2.1）编译烧录唯一主程序 `examples/FocKit_commissioning`，选板 ESP32 Dev Module | 0 error 烧录成功，串口 115200 出现整定主程序就绪信息与增益计算打印 | 验收报告 | **通过（2026-09-22 回填）**：P1 战役期间反复烧录，boot 三件套（就绪横幅/增益计算链/标定注入打印）日常可见 |
| P0-② | 力矩台阶响应 | 主程序 `loop t` 进电压力矩会话，`step on` 跑 ±0.01 N·m 方波（3s 一拍）；`stream` 采状态流 | 电机转动方向随力矩符号切换、幅度对称、无异常发热（1 分钟内电机仅温热） | 验收报告 + 波形与数据 | **通过（2026-09-22 回填）**：早期电压力矩冒烟（EXP-01 时代）＋`loop t` 会话全程使用；判据为定性三查，后被 TST-01 电流域验收实质取代 |
| P0-③ | 稳态速度精度 | 主程序 `loop v` 进速度环会话，`v 5` 给 5 rad/s 空载，`stream` 采 ≥30s | 原判据"5 rad/s 稳态误差 ≤5%、不持续振荡"——**修订（2026-09-22，依 TST-02/EXP-05）**：≥10 rad/s 稳态误差 ≤5% 且平滑跟踪；5~10 rad/s 为停-走边界带（硬件属性，边界记录不判失败） | 验收报告 + 波形与数据 | **通过（2026-09-21，TST-02）**：40 rad/s 档 σ≈3%；5 rad/s 停-走即 EXP-05 三重塌方机制（平滑边界 5~10 rad/s 已收口记录）——原判据写于实测之前，按边界知识修订留痕 |
| P0-④ | 标定断电固化 | 首次烧录（NVs 空）→ 完成标定 → 完全断电 → 重启 | 首烧打印"标定已固化"；重启打印"注入历史标定"且**无标定动作**（电机不动、initFOC 秒过） | 验收报告（两段串口日志对比） | **通过（2026-09-22 回填）**：NVS 固化/注入自早期起日常验证（历次重烧 boot 打印"注入历史标定"、initFOC 秒过即证据） |
| P0-⑤ | 契约零依赖 | `grep -rn "include.*SimpleFOC" src/core/ src/control/` | 零命中 | 验收报告（附命令输出） | 已通过（2026-09-16） |
| P0-⑥ | SimpleFOC Studio 连通 | 主程序串口输 `studio` 进会话 → 打开 SimpleFOCStudio（本地 EXE）选串口 115200 连接 | Studio 能连接并实时出曲线（目标/速度/角度）；在线改内置环增益后电机行为即时变化 | 验收报告 | **通过（2026-09-22 回填）**：Studio/整定台为 P1 战役主力工具（REF-12 §4.2 全程使用）；连接 SOP 见 REF-12 §5.1 |
| P0-⑦ | 位置环基本验收 | 主程序 `loop p` 进位置环会话，`step on` 跑 ±π/2 rad 方波（4s 一拍）；`stream` 采数据 | 阶跃收敛（稳态 ±0.1 rad 内），无持续超调/振荡 | 验收报告 + 波形与数据 | **被取代（通过）**：TST-03（2026-09-21）判据远严于本条——ess ±0.005 rad（vs 本条 ±0.1）、双向对称、两轮复现 |

**红线（全程有效；2026-09-22 勘正）**：供电 12V 额定（7.4~16V 范围），欠压阈值 11.1V；**持续电流上限 0.5A**（2026-09-17 官方规格 200~500 mA 取上限；2026-09-21 起固件 `DEFAULT_TORQUE_LIMIT=0.016 N·m`=0.5A×KT 兜底，commit `ffaf7fc`；原文"0.8A"系 v0.1.0 旧值），需动态余量时由上层放宽且峰值不超 4.5A；整定主程序空载运行，加载实验需新立验收条目。

## 2. P1 中间件完备

| 内容 | 验收方向 |
|---|---|
| 板载电流采样接入与真电流环（InlineCurrentSense，M0: 39/36、M1: 35/34） | 电流阶跃跟随、稳态误差达标（阈值立项时定）；KT 收口的前置。**✅ 已验收（2026-09-21，TST-01）：ωc=125 定档（Kp=0.53/Ki=1031），堵转判据 ess≤0.3%/Cd≤3%/纹波≤2%/双向对称 全绿，已抄回 `.ino`；证据 `验收报告/20260921_P1_电流环整定验收.md`、EXP-03** |
| 三环级联整定方法论（建模→带宽→增益→微调→抄回，计算优先） | 位置环阶跃超调可整定并复现；载体 = 整定主程序 FocKit_commissioning 计算区。**✅ 已验收（2026-09-21）：速度 TST-02（ωv=20/N=10，低速边界 5~10 记录）＋ 位置 TST-03（ωp=4，ess±0.005 双向对称）；证据 `验收报告/20260921_P1_速度环整定验收.md`、`20260921_P1_位置环整定验收.md`、EXP-04/05/06** |
| 目标斜坡（梯形速度曲线） | 斜坡跟踪误差达标（阈值【待填写：立项时定】） |
| 速度估计滤波对比（LPF Tf 扫描） | 整定建议有数据支撑 |
| 无感（SMO/PLL）接口预留 | 契约层预留位评审通过 |
| 参数收口（R/L 已由官方规格闭环；KT 待实测，见 HW-DENG §3） | KT 实测值落公式初值 0.0827 N·m/A 的合理带内并更新 bsp。**✅ 已收口（2026-09-21）：实测 0.032 落在带外——公式初值因输入 KV=100 失实废弃，四点定标抄回（commit `ffaf7fc`），EXP-04** |

**载体冻结（2026-09-22 起）**：三环整定载体 `examples/FocKit_commissioning`（**01 号示例程序**）随 P1 收官冻结——不再新增功能、不改已验收行为；转役**回归测试台**：此后 `src/` 库层任何改动（如挂账的主循环提速、SimpleFOC 2.3.x 升级）合入前，须以本程序回跑 TST-01/02/03 对应判据并全绿方可视为库改动合格。三环台自身挂账（整定台写入自动回读、大幅反转 velocity_limit 方案等）按挂账流程处置，不视为解冻。

## 3. P2 上层·力控（2026-09-22 立项：文档先行；同日载体改无连杆模式，理论/选型/判据机理见 REF-14）

载体 = **02 号示例程序** `examples/FocKit_force_ctrl`（第一轮已交付，2026-09-22）：**裸电机+手拨交互**（实验平台无连杆/机械臂，控制算法学习导向；轻杆+电子秤为升级路径）——在力矩通道（MT2+MC0）上与三环**平级**成环（虚拟重力前馈 / 阻抗律 K、D），不走位置/速度环上层（机理与理由见 REF-14 §1）；力矩通道带宽受底层时间常数约束（ωc=125），不提底层做不到的要求。上位机：整定台"力控"第四预设已随第一轮交付（ForceCard 写入读回＋实时读数，REF-14 §5；原"后置另立项"提前落地）。

| 条目 | 内容 | 测试方法 | 通过判据（**草案**，首验后定档） | 记录位置 | 状态 |
|---|---|---|---|---|---|
| T-P2-1 | setTorque 力矩通道抽测 ＋ 虚拟摆（虚拟重力前馈） | 裸电机两幕：①手按转子堵转，τ 阶梯 ±2/±5/±10 mN·m 对账（EXP-03 堵转纪律）；②虚拟摆 G_v=5 mN·m 手拨起摆（REF-14 §2.1） | ① 堵转 Iq vs 账面 τ/KT 偏差≤15% 且双向对称≤10%；② 振荡频率 vs √(G_v/J) 偏差≤15%；③ 半摆衰减≈2τ_f/G_v、停摆角≈asin(τ_f/G_v)（±30%，摩擦签名）；④ J=G_v/ω² vs 4.06e-6 对照≤15% | 验收报告（TST-04）＋ 波形与数据 | 待建载体 |
| T-P2-2 | 阻抗控制 T=K(θ\*−θ)−D·θ̇ | ωn=25（ωc/5，摩擦地板推窗口上沿）、ζ=1 → K=2.54e-3 N·m/rad、D=2.03e-4 N·m·s/rad（J=4.06e-6 定值直算，REF-14 §3.2）；慢推滞回曲线/手按保持 30s/拨过死区释放 | ① 滞回中线斜率=K ±25%、半宽≈τ_f ±50%（电流即力传感器）；② 手按偏转保持 30s Iq 漂移<5%（无积分顶墙证据）；③ 释放振铃≤1 次、ts≈4/(ζωn)≈0.16s 对照±50%（时基按 EXP-06 纪律探针节拍标定）；死区内（<20°）不回弹=摩擦地板边界注记 | 验收报告（TST-05）＋ 波形与数据 | 待建载体 |
| T-P2-3 | 虚拟墙（单边阻抗）：θ<θ_wall 自由区 T=0，越墙 T=−K_w·(θ−θ_wall)（D_w 可选） | K_w 起步=阻抗档 2.54e-3、硬墙扫描上限 5e-3（ωn=35 需 D_w 抑弹，REF-14 §4）；自由区手拨/墙内慢推/快速撞墙三幕 | ① 自由区 Iq 均值<0.02A（摩擦地板内）；② 墙内力-位移斜率=K_w ±25%；③ 撞墙无持续抖振、回弹≤1 次；④ 穿透<θ_b=τ_f/K_w 段无感=边界注记 | 验收报告（TST-06）＋ 波形与数据 | 待建载体（第二轮） |
| T-P2-5 | 柔顺轨迹：T=K(θ\*(t)−θ)−D·θ̇，梯形速度轨迹发生器（**还 T-P1-8 斜坡挂账**） | K/D 同阻抗档；梯形 ±π/2 往返（ω\*≈0.5 rad/s、加速度限幅）；恒速段读滞后/跟踪中手拨扰动/回轨 | ① 恒速段滞后 e=(τ_f+D·ω\*)/K≈0.35 rad 对照±50%（软弹簧+摩擦的稳态账，滞后是设计的）；② 扰动保持 2s Iq 漂移<5%、松手回轨 ~4/(ζωn) 量级；③ 梯形加减速段 θ̇ 斜率有界、无目标跳变（探针 θ\* 列直读） | 验收报告（TST-07）＋ 波形与数据 | 待建载体（第二轮） |

> **P2 收官口径（2026-09-22 与用户确认）**：四件（T-P2-1/2/3/5）验收关门；**砍件留痕**——惯量整形（θ̈ 双差分噪声本硬件不可验证）、观测器力位混合（摩擦模型不确定度 ~0.5 mN·m vs 手拨力 mN·m 级，信噪比不支撑，降级 P3 可选）；经典力位混合（Raibert-Craig 任务空间分解）需 ≥2 自由度+力传感器，留实验室臂时代。程序两轮交付：第一轮骨架+raw/pend/imp（验收 1/2），第二轮+wall/traj（验收 3/5）——载体与切分见 REF-14 §6。

## 4. P3 辨识与多关节

惯量/摩擦辨识（STM32 方法论迁移）、CAN 多关节。验收方向：辨识参数交叉验证。【待填写】

## 5. P4 组合适配

`LabCanMotor` 适配器、实验室硬件联调。验收方向：上层算法零改动切换平台（以 P0~P3 验收条目在新平台复跑为判据）。【待填写】
