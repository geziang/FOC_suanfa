# 阶段验收计划 P0~P4

> 状态：P0 已对齐（代码就绪，待上板验收）；**P1 三环已全验收（电流 TST-01 / 速度 TST-02 / 位置 TST-03，2026-09-21）**；**P2 力控已立项（2026-09-22 文档先行，判据草案，见 §3 与 REF-14）**；其余 P1 细项与 P3~P4 草稿 ｜ 文档编号：SPEC-P
> 最后对齐：2026-09-22（P2 立项：§3 条目化 + §2 三环载体冻结；同日 §3 按实验平台约束改无连杆模式：虚拟摆+阻抗、判据重写，REF-14 同步；前一里程碑 2026-09-21 P1 三环收官：T-P1-3 电流 ωc=125 ／ T-P1-6 速度 ωv=20·N=10+KT 定标 ／ T-P1-7 位置 ωp=4）｜ 上游：REQ-01、ARC-01、SPEC-T

---

## 1. P0 套件载体（现行阶段）

代码已全部就绪（git `28a66ef`，FocKit v0.1.0），验收条目如下。**每条验收后按 `06_验证与实验记录/验收报告/` 模板落记录，本表回填实测状态。**

| 条目 | 内容 | 测试方法 | 通过判据 | 记录位置 | 状态 |
|---|---|---|---|---|---|
| P0-① | 主程序编译烧录 | Arduino IDE（ESP32 core ≥2.0.4 + Simple FOC ≥2.2.1）编译烧录唯一主程序 `examples/FocKit_commissioning`，选板 ESP32 Dev Module | 0 error 烧录成功，串口 115200 出现整定主程序就绪信息与增益计算打印 | 验收报告 | 待上板 |
| P0-② | 力矩台阶响应 | 主程序 `loop t` 进电压力矩会话，`step on` 跑 ±0.01 N·m 方波（3s 一拍）；`stream` 采状态流 | 电机转动方向随力矩符号切换、幅度对称、无异常发热（1 分钟内电机仅温热） | 验收报告 + 波形与数据 | 待上板 |
| P0-③ | 稳态速度精度 | 主程序 `loop v` 进速度环会话，`v 5` 给 5 rad/s 空载，`stream` 采 ≥30s | 稳态速度误差 ≤5%（5±0.25 rad/s），不持续振荡 | 验收报告 + 波形与数据 | 待上板 |
| P0-④ | 标定断电固化 | 首次烧录（NVs 空）→ 完成标定 → 完全断电 → 重启 | 首烧打印"标定已固化"；重启打印"注入历史标定"且**无标定动作**（电机不动、initFOC 秒过） | 验收报告（两段串口日志对比） | 待上板 |
| P0-⑤ | 契约零依赖 | `grep -rn "include.*SimpleFOC" src/core/ src/control/` | 零命中 | 验收报告（附命令输出） | 已通过（2026-09-16） |
| P0-⑥ | SimpleFOC Studio 连通 | 主程序串口输 `studio` 进会话 → 打开 SimpleFOCStudio（本地 EXE）选串口 115200 连接 | Studio 能连接并实时出曲线（目标/速度/角度）；在线改内置环增益后电机行为即时变化 | 验收报告 | 待上板 |
| P0-⑦ | 位置环基本验收 | 主程序 `loop p` 进位置环会话，`step on` 跑 ±π/2 rad 方波（4s 一拍）；`stream` 采数据 | 阶跃收敛（稳态 ±0.1 rad 内），无持续超调/振荡 | 验收报告 + 波形与数据 | 待上板 |

**红线（全程有效）**：供电 12V 额定（7.4~16V 范围），欠压阈值 11.1V；电流上限默认额定 0.8A（需动态余量时由上层放宽，峰值不超 4.5A）；整定主程序空载运行，加载实验需新立验收条目。

## 2. P1 中间件完备

| 内容 | 验收方向 |
|---|---|
| 板载电流采样接入与真电流环（InlineCurrentSense，M0: 39/36、M1: 35/34） | 电流阶跃跟随、稳态误差达标（阈值立项时定）；KT 收口的前置。**✅ 已验收（2026-09-21，TST-01）：ωc=125 定档（Kp=0.53/Ki=1031），堵转判据 ess≤0.3%/Cd≤3%/纹波≤2%/双向对称 全绿，已抄回 `.ino`；证据 `验收报告/20260921_P1_电流环整定验收.md`、EXP-03** |
| 三环级联整定方法论（建模→带宽→增益→微调→抄回，计算优先） | 位置环阶跃超调可整定并复现；载体 = 整定主程序 FocKit_commissioning 计算区。**✅ 已验收（2026-09-21）：速度 TST-02（ωv=20/N=10，低速边界 5~10 记录）＋ 位置 TST-03（ωp=4，ess±0.005 双向对称）；证据 `验收报告/20260921_P1_速度环整定验收.md`、`20260921_P1_位置环整定验收.md`、EXP-04/05/06** |
| 目标斜坡（梯形速度曲线） | 斜坡跟踪误差达标（阈值【待填写：立项时定】） |
| 速度估计滤波对比（LPF Tf 扫描） | 整定建议有数据支撑 |
| 无感（SMO/PLL）接口预留 | 契约层预留位评审通过 |
| 参数收口（R/L 已由官方规格闭环；KT 待实测，见 HW-DENG §3） | KT 实测值落公式初值 0.0827 N·m/A 的合理带内并更新 bsp。**✅ 已收口（2026-09-21）：实测 0.032 落在带外——公式初值因输入 KV=100 失实废弃，四点定标抄回（commit `ffaf7fc`），EXP-04** |

**载体冻结（2026-09-22 起）**：三环整定载体 `examples/FocKit_commissioning`（**01 号示例程序**）随 P1 收官冻结——不再新增功能、不改已验收行为；转役**回归测试台**：此后 `src/` 库层任何改动（如挂账的主循环提速、SimpleFOC 2.3.x 升级）合入前，须以本程序回跑 TST-01/02/03 对应判据并全绿方可视为库改动合格。三环台自身挂账（整定台写入自动回读、大幅反转 velocity_limit 方案等）按挂账流程处置，不视为解冻。

## 3. P2 上层·力控（2026-09-22 立项：文档先行；同日载体改无连杆模式，理论/选型/判据机理见 REF-14）

载体 = **02 号示例程序** `examples/FocKit_force_ctrl`（阶段二另走需求确认后动工）：**裸电机+手拨交互**（实验平台无连杆/机械臂，控制算法学习导向；轻杆+电子秤为升级路径）——在力矩通道（MT2+MC0）上与三环**平级**成环（虚拟重力前馈 / 阻抗律 K、D），不走位置/速度环上层（机理与理由见 REF-14 §1）；力矩通道带宽受底层时间常数约束（ωc=125），不提底层做不到的要求。上位机第一级零改动（现 Studio+整定台量化包），力控预设卡后置另立项（REF-14 §5）。

| 条目 | 内容 | 测试方法 | 通过判据（**草案**，首验后定档） | 记录位置 | 状态 |
|---|---|---|---|---|---|
| T-P2-1 | setTorque 力矩通道抽测 ＋ 虚拟摆（虚拟重力前馈） | 裸电机两幕：①手按转子堵转，τ 阶梯 ±2/±5/±10 mN·m 对账（EXP-03 堵转纪律）；②虚拟摆 G_v=5 mN·m 手拨起摆（REF-14 §2.1） | ① 堵转 Iq vs 账面 τ/KT 偏差≤15% 且双向对称≤10%；② 振荡频率 vs √(G_v/J) 偏差≤15%；③ 半摆衰减≈2τ_f/G_v、停摆角≈asin(τ_f/G_v)（±30%，摩擦签名）；④ J=G_v/ω² vs 4.06e-6 对照≤15% | 验收报告（TST-04）＋ 波形与数据 | 待建载体 |
| T-P2-2 | 阻抗控制 T=K(θ\*−θ)−D·θ̇ | ωn=25（ωc/5，摩擦地板推窗口上沿）、ζ=1 → K=2.54e-3 N·m/rad、D=2.03e-4 N·m·s/rad（J=4.06e-6 定值直算，REF-14 §3.2）；慢推滞回曲线/手按保持 30s/拨过死区释放 | ① 滞回中线斜率=K ±25%、半宽≈τ_f ±50%（电流即力传感器）；② 手按偏转保持 30s Iq 漂移<5%（无积分顶墙证据）；③ 释放振铃≤1 次、ts≈4/(ζωn)≈0.16s 对照±50%（时基按 EXP-06 纪律探针节拍标定）；死区内（<20°）不回弹=摩擦地板边界注记 | 验收报告（TST-05）＋ 波形与数据 | 待建载体 |

## 4. P3 辨识与多关节

惯量/摩擦辨识（STM32 方法论迁移）、CAN 多关节。验收方向：辨识参数交叉验证。【待填写】

## 5. P4 组合适配

`LabCanMotor` 适配器、实验室硬件联调。验收方向：上层算法零改动切换平台（以 P0~P3 验收条目在新平台复跑为判据）。【待填写】
