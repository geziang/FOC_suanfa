# 阶段验收计划 P0~P4

> 状态：P0 已对齐（代码就绪，待上板验收）；P1~P4 草稿 ｜ 文档编号：SPEC-P
> 最后对齐：2026-09-17（补 P0-⑦ 位置环与 P1 电流环条目；执行顺序与门禁见 SPEC-T）｜ 上游：REQ-01、ARC-01、SPEC-T

---

## 1. P0 套件载体（现行阶段）

代码已全部就绪（git `28a66ef`，FocKit v0.1.0），验收条目如下。**每条验收后按 `06_验证与实验记录/验收报告/` 模板落记录，本表回填实测状态。**

| 条目 | 内容 | 测试方法 | 通过判据 | 记录位置 | 状态 |
|---|---|---|---|---|---|
| P0-① | 三示例编译烧录 | Arduino IDE（ESP32 core ≥2.0.4 + Simple FOC ≥2.2.1）逐个编译烧录 `examples/01~03`，选板 ESP32 Dev Module | 0 error 烧录成功，串口 115200 出现 FocKit 就绪信息 | 验收报告 | 待上板 |
| P0-② | 力矩台阶响应 | 示例 01 上电，观察每 3 秒 ±0.01 N·m 台阶；`stream` 采状态流 | 电机转动方向随力矩符号切换、幅度对称、无异常发热（1 分钟内电机仅温热） | 验收报告 + 波形与数据 | 待上板 |
| P0-③ | 稳态速度精度 | 示例 02，目标 5 rad/s 空载，`stream` 采 ≥30s | 稳态速度误差 ≤5%（5±0.25 rad/s），不持续振荡 | 验收报告 + 波形与数据 | 待上板 |
| P0-④ | 标定断电固化 | 首次烧录（NVs 空）→ 完成标定 → 完全断电 → 重启 | 首烧打印"标定已固化"；重启打印"注入历史标定"且**无标定动作**（电机不动、initFOC 秒过） | 验收报告（两段串口日志对比） | 待上板 |
| P0-⑤ | 契约零依赖 | `grep -rn "include.*SimpleFOC" src/core/ src/control/` | 零命中 | 验收报告（附命令输出） | 已通过（2026-09-16） |
| P0-⑥ | SimpleFOC Studio 连通 | 示例 04：串口输 `studio` 进会话 → 打开 SimpleFOCStudio（本地 EXE）选串口 115200 连接 | Studio 能连接并实时出曲线（目标/速度/角度）；在线改内置环增益后电机行为即时变化 | 验收报告 | 待上板 |
| P0-⑦ | 位置环基本验收 | 示例 03，观察每 4 秒 0↔π rad 目标切换；`stream` 采数据 | 阶跃收敛（稳态 ±0.1 rad 内），无持续超调/振荡 | 验收报告 + 波形与数据 | 待上板 |

**红线（全程有效）**：供电 12V 额定（7.4~16V 范围），欠压阈值 11.1V；电流上限默认额定 0.8A（需动态余量时由上层放宽，峰值不超 4.5A）；示例 01~03 空载运行，加载实验需新立验收条目。

## 2. P1 中间件完备

| 内容 | 验收方向 |
|---|---|
| 板载电流采样接入与真电流环（InlineCurrentSense，M0: 39/36、M1: 35/34） | 电流阶跃跟随、稳态误差达标（阈值立项时定）；KT 收口的前置。**接入代码已就绪（2026-09-17，示例 05 载体）** |
| 三环级联整定方法论（建模→带宽→增益→微调→抄回，计算优先） | 位置环阶跃超调可整定并复现；载体 = 示例 05 计算区 |
| 目标斜坡（梯形速度曲线） | 斜坡跟踪误差达标（阈值【待填写：立项时定】） |
| 速度估计滤波对比（LPF Tf 扫描） | 整定建议有数据支撑 |
| 无感（SMO/PLL）接口预留 | 契约层预留位评审通过 |
| 参数收口（R/L 已由官方规格闭环；KT 待实测，见 HW-DENG §3） | KT 实测值落公式初值 0.0827 N·m/A 的合理带内并更新 bsp |

## 3. P2 上层·力控

重力补偿、阻抗控制（力矩通道带宽受底层时间常数约束，不提底层做不到的要求）。验收方向：阻抗参数可整定、接触稳定。【待填写：立项时细化条目】

## 4. P3 辨识与多关节

惯量/摩擦辨识（STM32 方法论迁移）、CAN 多关节。验收方向：辨识参数交叉验证。【待填写】

## 5. P4 组合适配

`LabCanMotor` 适配器、实验室硬件联调。验收方向：上层算法零改动切换平台（以 P0~P3 验收条目在新平台复跑为判据）。【待填写】
