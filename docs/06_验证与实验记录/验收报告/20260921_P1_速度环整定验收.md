# P1 速度环整定验收报告（T-P1-6）

> 编号：TST-02 ｜ 状态：**通过（PASS，附低速边界记录与两项挂账）** ｜ 验收日期：2026-09-21
> 对应合同：SPEC-T T-P1-6（前置：T-P1-3 电流环冻结 ωc=125，TST-01）
> 定档结论：**ωv = 20 rad/s、N = 10**——kp_v = ωv·J/KT = **0.0025 A/(rad/s)**、ki_v = N×kp = **0.0254**、输出限幅 **0.5A**（持续红线钳位）、Tf = 10 ms；随役参数定标 **KT = 0.032 N·m/A、J = 4.06e-6 kg·m²（②实测）**——已抄回 `FocKit_commissioning.ino`（kiV 倍率/J_EST/KT 链）与 `DengFocBoard.h`（commit `ffaf7fc`）
> 证据：`../波形与数据/速度环_N10扫速5至40_定标.csv` + `速度环_N10扫速_VELDBG探针日志.txt`（A 级，已落盘）；低速对照 `速度环_低速3rad_s_实际N5_停走锯齿.csv`；根因与过程见 EXP-04 / EXP-05
> 测试条件：整定台「速度环」预设（MT2 foc_current + MC1，原子模式保证）；5/10/20/30/40 rad/s 稳态扫描 + 阶跃；曲线降采样 100；**探针 [VEL DBG] 500ms 在线，写入经探针 P=/I= 字段验证落地**（纪律见 EXP-04 §0）

## 1. 判据—实测对照（N=10，2026-09-21）

| 判据 | 工况 | 实测 | 判定 |
| --- | --- | --- | --- |
| 平滑跟踪 | 10 rad/s | ±2 rad/s 纹波、无卡死拍 | 通过 |
| 平滑跟踪 | 20 / 30 / 40 rad/s | 紧贴目标（40 档 σ≈1.3 ≈ 3%） | 通过 |
| 模式防线 | 全程 | 固件模式指示=velocity+foc_current；探针 ctrl=1/tt=2 一致 | 通过 |
| 写入验证 | ki 写入后 | 探针 I=0.0254 全程 + 六条命令回显 | 通过 |
| 增益零漂移 | 定标抄回后复算 | kpV = 20×4.06e-6/0.032 = 0.00254，与在线整定值一致 | 通过 |
| 低速边界 | 5 rad/s | 停-走锯齿（Vel 0.1~17.3、均值≈5）——**定性为停-走区，不判失败**（EXP-05） | 边界记录 |
| 低速边界（对照） | 3 rad/s @N=5 | 停-走锯齿（46% 卡死 / 32% 冲刺，均值 3.06≈目标） | 边界记录 |

**验收范围**：**≥10 rad/s 平滑跟踪验收通过**；5~10 rad/s 为停-走边界带（机制=三重塌方，EXP-05），<5 rad/s 未测。低速场景（如云台缓动）若需 <10 rad/s 平滑，走 §4 挂账的升级路线，不回退整定。

## 2. 过程要点（详见 EXP-04 / EXP-05）

- 失控案三重防线（sendTarget 原子重发 MT+MC / 固件模式指示 / 级联限幅 0.5A）全程有效，未再复现 2026-09-21 早失控；
- 首轮"N=10 失败"系写入未生效的假象（参数卡显示本地对象值≠固件状态），重写并经探针验证后定档——写入必读回自此立为纪律；
- KT/J 定标与速度环收官同役完成（反电动势四点法，零新增硬件）：KE=0.0320±0.002，连带 DEFAULT_TORQUE_LIMIT 安全更正 0.03→0.016 N·m；
- N 扫描本质=阻尼比扫描（ζ=½√(ωv/N)）：N=10 → ζ=0.707（PM 实际 ≈43°），线性区一枪收敛验证"设计一次、上机只验证"。

## 3. 抄回记录（commit `ffaf7fc`，已推）

| 项 | 旧值 | 新值 | 位置 |
| --- | --- | --- | --- |
| kiV 倍率 N | 5.0f | **10.0f**（ζ=0.707） | `.ino` computeGains |
| J_EST | 1.05e-5（③反推） | **4.06e-6**（②实测等比更正，保 kpV 零漂移） | `.ino` |
| kt | 0.0827（公式初值） | **0.032**（②四点定标，残差±3%） | `DengFocBoard.h` |
| DEFAULT_TORQUE_LIMIT | 0.03 N·m | **0.016 N·m**（=0.5A×0.032，红线一致） | `DengFocBoard.h`（安全连带） |

**待重烧生效**：boot 打印应见 `KT=0.0320 / J=4.06e-06 / kp_v=0.0025 / ki_v=0.0254`——即零漂移验证点。

## 4. 挂账移交（不阻塞本验收）

1. **80 档读数悬案 [D-存疑]**：Vq 物理账反推真速≈50 而报告 80，疑似传感器链在真速 40~50 以上放大——80 档跑探针看 rot 增量定音（真 80 → +6~7 圈/0.5s）；未决前高速区结论限于 ≤40 rad/s；
2. 重烧 boot 打印验证 + setTorque/state_.torque 力矩换算上机抽测（EXP-04 修愈确认）；
3. 速度环 PID 限幅被 SimpleFOC foc_current 绑到 current_limit=2.0A（0.5A 覆盖失效）——MLC 收口；
4. 整定台"写入后自动回读、不符标红"小改（防 UI 假象复发，EXP-04 §8）；
5. **位置环 T-P1-7**（下一站，同样级联 MT2+MC2，剧本见 REF-12 §4.2）。
