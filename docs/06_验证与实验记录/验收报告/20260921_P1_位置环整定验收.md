# P1 位置环整定验收报告（T-P1-7）

> 编号：TST-03 ｜ 状态：**通过（PASS，附两项放弃记录与挂账移交）** ｜ 验收日期：2026-09-21
> 对应合同：SPEC-T T-P1-7（前置：T-P1-6 速度环冻结 ωv=20/N=10，TST-02）
> 定档结论：**ωp = kpP = ωv/5 = 4 [1/s]（P-only）**——自级联版起已在固件 boot 注入（`WP` 常数，本轮**无抄回动作**）；**级联架构 MT2+MC2**（位置 P 输出=速度指令 → 冻结速度环 → 电流环），预设电压域遗留错配已修复（commit `e0a0d93`，修复验证见 §1 模式防线行）
> 证据：`../波形与数据/位置环_P4级联_单步首审.csv`、`位置环_P4级联_复现与负向对称.csv`、`位置环_P4级联_单步探针日志.txt`（A 级，已落盘）；电压域对照 `位置环_电压域_P4全反转张弛振荡.csv`、`位置环_电压域_多P快翻转乱象.csv`；根因与过程见 EXP-06
> 测试条件：整定台「位置环」预设（修复后 MT2+MC2），探针 `ctrl=2 tt=2` 全程确认；0→±π/2 单步（阶跃前稳零）；**mds=8**（运动环 ~170Hz、电流环 1.35kHz 全速——见放弃记录①）

## 1. 判据—实测对照（P=4，2026-09-21）

| 判据 | 工况 | 实测 | 判定 |
| --- | --- | --- | --- |
| 上升时间 tr | 0→+π/2 单步 | **~0.5s**（探针两行夹出） | 通过（一阶预测 0.55s 命中） |
| 超调 | 同上 | 首审 6.4% / 复审采样 ~26%——峰值受破摩强度与采样混叠逐轮波动，**判据锚定终值指标** | 通过（终值两轮一致） |
| 稳态误差 ess | +π/2 | **+0.003 rad**（稳态带 1.5693~1.5754） | 通过 |
| 稳态误差 ess | -π/2（负向对称） | **-0.005 rad**（抖动 ±0.005） | 通过（双向对称；电压域时代 0.084 不对称已消失） |
| 稳态带驻留 | 双向 | 抖动 ±0.005~0.04 rad ≪ ±0.1 rad 判据带 | 通过 |
| 终值段行为 | 到位后 | 微抖、无极限环；**终值爬行 ~7s**（停-走区投影，与 EXP-05 预测同向、量级记录在案） | 边界记录 |
| 恒载保持 | 保持段 | 探针 `vq=-0.13V / Cq=-0.02A` 持续对抗齿槽拉力而 ess≈0 | 通过（type-1 环实证） |
| 复现 | 两轮（首审+复审） | 终值指标一致 | 通过 |
| 模式防线 | 全程 | 探针 ctrl=2 tt=2；指示灯 位置/FOC电流 | 通过 |

**验收范围**：从静止出发的 ±π/2 小步定位。大幅全反转工况（动能过冲/fling）**未纳入本次验收**——应用需要时以 velocity_limit 软限速方案另行验收（见挂账 1）。

## 2. 过程要点（详见 EXP-06）

- 首轮"P=4 大过冲"实为**预设电压域遗留错配**（MT0 下电流域速度环增益失效 → 纯积分+2V 钳位 → 张弛振荡）：模式指示灯（失控案防线 #2）与探针联合破案，修复 `e0a0d93`；
- P-only 设计实证：环内自带积分（位置 1/s + 速度环 I）→ 恒齿槽载 ess≈0；"不加 I"的物理证据=保持段反向力矩由速度环积分器输出；
- 判读纪律两次立功：P/I/D 读回钉死（EXP-04 纪律复用）；采样时基以探针节拍当次标定（修正两次误判，入 EXP-06 坟场）。

## 3. 抄回记录

无——`WP=4` 自级联版（`7e03967`）起已在固件 boot 注入，本轮全程未改增益。相关变更：位置预设级联化 + REF-12 §4.2 剧本重写 + .ino J 标签润色（`e0a0d93`）。

## 4. 放弃记录（主动，非缺陷）

1. **mds=0 复测**：本验收在 mds=8 下取证。T-P1-6 的证据本身横跨 mds=0（40 档验证）与 mds=10（N10 扫速），**mds=8 落在已验证区间内被夹住**，形式复测放弃，验收条件如文头注明；
2. **P=20 对照**（官方基线=零分离比反面教材）：EXP 素材可选项，未做。预测"P=20 超调振铃"记**未验证**，两分钟可补。

## 5. 挂账移交（不阻塞本验收）

1. **大幅全反转工况**：动能过冲/fling 未验收——需要时先试 velocity_limit 30→8 软限速（一条命令），再议小 D（0.02 起）；
2. **终值爬行 ~7s**：云台缓动场景评估接受性；
3. **80 档读数悬案 [D]**（传感器爆发段可信度）与判读时基纪律相关，跨战役遗留；
4. 整定台"写入后自动回读"小改（EXP-04 遗留）；
5. **P2 力控**（下一站）——setTorque 力矩换算已随 KT 定标（`ffaf7fc`）痊愈，上机抽测仍欠。
